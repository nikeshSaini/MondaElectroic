<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feedback Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            margin-top: 20px;
        }

        form {
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"],
        textarea {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        textarea {
            resize: vertical;
            height: 100px;
        }

        button[type="submit"] {
            background-color: #4caf50;
            color: #fff;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
        }

        button[type="submit"]:hover {
            background-color: #45a049;
        }

        nav {
            background-color: #333;
            color: #fff;
            height: 65px;
            padding: 10px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 10px;
        }
        nav a {
            color: #fff;
            text-decoration: none;
            margin-right: 20px;
        }
        .right-side {
            display: flex;
            align-items: baseline;
        }
        .right-side a img{
            height: 15px;
        }
        .container {
            text-align: center;
            margin-top: 50px;
        }
        .button {
            padding: 10px 20px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            margin: 10px;
        }
        .button:hover {
            background-color: #0056b3;
        }
        img{
            height: 45px;
        }
        .left-side{
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* Responsive styles */
        @media screen and (max-width: 600px) {
            form {
                padding: 10px;
            }
            input[type="text"],
            input[type="number"],
            textarea {
                padding: 6px;
            }
            button[type="submit"] {
                padding: 8px 16px;
                font-size: 14px;
            }
        }

        
    </style>
</head>
<body>
    <nav>
        <div class="left-side">
            <a href="/"><img src="./logo.jpeg" alt=""></a>
            
        </div>
        <div class="right-side">
            <!-- Placeholder for user name -->
            <a href="/logout"><img src="./logout.svg" alt=""> Logout</a> <!-- Add Logout link when user is logged in -->
            <span style="margin-left: 10px;" id="username-placeholder"><%= userCred.email %></span>
            
        </div>
    </nav>

    <h1>Feedback Form</h1>
    
    <form id="feedbackForm" action="/feedback" method="post" enctype="multipart/form-data">
        <!-- Hidden input field to include the user's ID -->
        <input type="hidden" id="userId" name="feedback[userId]" value="<%=  userCred._id %>">
        
        <label for="venue">Venue Name:</label>
        <input type="text" id="venue" name="feedback[venue]" required>
        
       
        <label for="expenditure">Expenditure:</label>
        <input type="number" id="expenditure" name="feedback[expenditure]" required>
        
        <label for="location">Location:</label>
        <input type="text" id="location" name="feedback[location]" required>
        
        <label for="description">Description:</label>
        <textarea id="description" name="feedback[description]" rows="4" cols="50" required></textarea>
        <label for="image">Image:</label>
        <input type="file" id="image" name="pageImg" accept="image/*" required>
        

        <button type="submit">Submit</button>
    </form>
</body>
</html>
